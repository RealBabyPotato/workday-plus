# import

this folder is for data import scripts later.

what could go here:

- ubc course data imports
- degree requirement imports
- building and room data imports

why this exists:

- imported data is a backend-heavy area with real value
- keeping import logic separate makes it easier to test and rerun

install:

- no separate install
- ignore this folder until import scripts are added

launch:

- no launch right now
- run import scripts directly from this folder once they exist
