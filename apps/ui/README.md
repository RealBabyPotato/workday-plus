# ui

this folder is optional later work.

for the lean v1 scaffold, this is not required.

why it still exists:

- if the frontend grows large, shared design-system components can live here
- for now, keeping ui code inside `apps/web` is simpler

install:

- do not install this yet
- ignore this folder for v1 unless the team decides to build a separate shared component package

launch:

- no launch for v1
- ignore this folder unless a separate ui package becomes necessary
