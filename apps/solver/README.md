# solver

this folder is optional later work.

for the lean v1 scaffold, this is not required.

why it still exists:

- if the team later wants a stronger schedule generator or c++ integration, this is the place for it
- keeping it separate avoids forcing extra complexity into the main backend too early

install:

- do not install this yet
- ignore this folder for v1 unless the team explicitly chooses to build an advanced scheduler

launch:

- no launch for v1
- ignore this folder unless you later decide to build a separate solver
